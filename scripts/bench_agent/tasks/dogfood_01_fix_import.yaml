name: dogfood_01_fix_import
category: dogfood
prompt: |
  仓库中有一处 ImportError（模拟：缺失的相对导入）。
  请定位并修复，保证相关测试可收集。
  要求：先读再改，不要重写整文件。
budget:
  max_iterations: 25
assertions:
  - type: max_iterations
    value: 25
