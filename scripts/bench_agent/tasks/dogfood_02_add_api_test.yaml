name: dogfood_02_add_api_test
category: dogfood
prompt: |
  为 GET /runs 或 memory_bus 相关路径补充/完善一条 pytest 回归测试。
  保持最小改动，测试需可独立运行。
budget:
  max_iterations: 30
assertions:
  - type: max_iterations
    value: 30
