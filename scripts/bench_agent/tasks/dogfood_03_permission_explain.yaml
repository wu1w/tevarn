name: dogfood_03_permission_explain
category: dogfood
prompt: |
  说明 permission_court 如何对 file_read 给出 layer/matched_rule。
  可阅读代码后用文字总结，不必改代码。
budget:
  max_iterations: 20
assertions:
  - type: max_iterations
    value: 20
