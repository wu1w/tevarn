# Tevarn backend dependencies
# Pin versions for reproducible desktop packaging
# NOTE: comments must stay ASCII-only - pip decodes this file with the OS
# locale encoding (GBK on zh-CN Windows), non-ASCII bytes can crash install.

fastapi==0.139.0
uvicorn[standard]==0.50.2
pydantic==2.13.4
pydantic-settings>=2.2.1
email-validator>=2.2.0
sqlalchemy[asyncio]==2.0.51
aiosqlite==0.21.0
asyncpg==0.30.0
cryptography>=42.0.0
# core/security.py imports jwt directly - must be declared explicitly,
# never rely on transitive deps
PyJWT>=2.8,<3
passlib[bcrypt]==1.7.4
# Upper bound is a hard constraint: bcrypt>=4.1 removed __about__ which
# passlib 1.7.4 depends on; broken combo makes every password fail with
# "longer than 72 bytes" and login breaks entirely.
bcrypt>=4.0.1,<4.1
python-multipart==0.0.31
httpx==0.28.1
aiohttp==3.14.1
websockets==15.0.1
python-dotenv==1.2.2
pyyaml==6.0.2
jinja2==3.1.6
qdrant-client==1.12.1
pytest==8.3.5
pytest-asyncio==0.24.0
asgi-lifespan==2.1.0
mcp>=1.12.0
croniter==6.2.4

alembic>=1.13  # schema migration (introduced in 0.4.6-alpha)
