# Takton backend - production / desktop packaging only
# Pin versions for reproducible desktop packaging
# Dev/test deps live in requirements-dev.txt
# NOTE: comments must stay ASCII-only - pip decodes this file with the OS
# locale encoding (GBK on zh-CN Windows), non-ASCII bytes can crash install.

fastapi==0.139.0
uvicorn[standard]==0.50.2
pydantic==2.13.4
pydantic-settings>=2.2.1
email-validator>=2.2.0
# 2.0.36 breaks on Python 3.14 (Union typing); pin to a 3.14-safe release.
sqlalchemy[asyncio]==2.0.51
aiosqlite==0.21.0
PyJWT==2.13.0
cryptography>=42.0.0
passlib[bcrypt]==1.7.4
# Hard upper bound: bcrypt>=4.1 removed __about__ which passlib 1.7.4
# depends on; broken combo makes ANY password fail with the 72-bytes error,
# breaking login/register/single-user init entirely.
bcrypt>=4.0.1,<4.1
python-multipart==0.0.31
httpx==0.28.1
aiohttp==3.14.1
websockets==15.0.1
python-dotenv==1.2.2
pyyaml==6.0.2
jinja2==3.1.6
qdrant-client==1.12.1
mcp>=1.12.0
croniter>=2.0.0
# Multi-worker kernel shared state (optional; needed when
# agent_kernel_redis_shared=true)
redis>=5.0.0

tzdata>=2024.1  # Windows ZoneInfo / current_time
pywin32>=311; sys_platform == "win32"
# Free web_search
ddgs>=9.0.0
