[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tevarn-code"
version = "0.1.0"
description = "Tevarn Code — repo-native coding agent (independent; desktop bridge ready)"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Tevarn" }]
dependencies = [
  "httpx>=0.27",
  "rich>=13.7",
  "typer>=0.12",
  "pydantic>=2.6",
  "pydantic-settings>=2.2",
  "aiosqlite>=0.20",
  "gitpython>=3.1",
  "prompt-toolkit>=3.0",
  "textual>=1.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23"]

[project.scripts]
tevarn-code = "tevarn_code.cli:app"
tkc = "tevarn_code.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["src/tevarn_code"]

[tool.hatch.build.targets.wheel.force-include]
"src/tevarn_code" = "tevarn_code"

[tool.hatch.build.targets.sdist]
include = ["src/tevarn_code", "README.md", "pyproject.toml", "tests", "smoke", "fixtures"]
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.uv]
package = true
